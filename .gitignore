# LaTeX build artifacts — the compiled PDF and aux files are generated
# (by Overleaf or a local pdflatex/latexmk run) and are not tracked.
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.glg
*.glo
*.gls
*.idx
*.ilg
*.ind
*.lof
*.log
*.lot
*.nav
*.out
*.pdf
*.run.xml
*.snm
*.synctex.gz
*.toc
*.vrb
*.xdy

# Editor / OS noise
*.swp
.DS_Store

# Review/audit working notes and AI-tooling metadata — never part of the submission.
AUDIT_*
FIX_PLAN_*
*_REVIEW_FINDINGS.md
*_FINDINGS.md
.claude/

# latexmk error-recovery artifacts
*-SAVE-ERROR
_to_delete/

# Local scratch (renders, logs, source captures) — never part of the submission.
tmp/
