% fig-eval-attacktree.tex -- attack routes and their detection controls.
% Two parallel groups under matching headers: left, the A1/A3/A5 substitution
% routes (an OR: any one reaches the goal); right, the A2/A4 detection-evasion
% attempts. Each attack box sits above the control that detects it (dashed
% arrow). X1 (endpoint compromise) bypasses the directory and is out of scope.
% Label: \label{fig:eval-attacktree}
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[>=Stealth,font=\scriptsize,
  goal/.style={draw,thick,rounded corners,fill=black!6,align=center,text width=3.6cm,minimum height=9mm,inner sep=3pt},
  hdr/.style={draw,rounded corners,fill=blue!8,draw=blue!55!black,align=center,font=\scriptsize\bfseries,text width=3.7cm,minimum height=7mm,inner sep=3pt},
  atk/.style={draw,rounded corners,draw=red!60!black,align=center,text width=2.4cm,minimum height=10mm,inner sep=3pt},
  det/.style={draw,rounded corners,draw=green!45!black,fill=green!5,text=green!25!black,align=center,text width=2.4cm,minimum height=12mm,inner sep=3pt},
  oos/.style={draw,dashed,rounded corners,draw=orange!80!black,align=center,minimum height=8mm,inner sep=4pt}]
  \hyphenpenalty=10000\exhyphenpenalty=10000  % no mid-word breaks in the boxes

  % two parallel, matching group headers at the same level
  \node[hdr] (orG)   at (-2.0, 3.7) {OR: any one substitution route};
  \node[hdr] (rhead) at ( 4.8, 3.7) {Detection-evasion attempts};

  % attack rows (uniform boxes)
  \node[atk] (a1) at (-4.7, 2.0) {A1\\server substitution};
  \node[atk] (a3) at (-2.0, 2.0) {A3\\network MitM};
  \node[atk] (a5) at ( 0.7, 2.0) {A5\\malicious rotation};
  \node[atk] (a2) at ( 3.5, 2.0) {A2\\warning suppression};
  \node[atk] (a4) at ( 6.1, 2.0) {A4\\equivocation (split view)};
  \foreach \n in {a1,a3,a5}{\draw[->,black!45] (orG) -- (\n);}
  \draw[->,black!45] (rhead) -- (a2);
  \draw[->,black!45] (rhead) -- (a4);

  % detection rows (uniform boxes, short labels)
  \node[det] (d1) at (-4.7,-0.2) {owner self-check or established-peer pin};
  \node[det] (d3) at (-2.0,-0.2) {global VRF identity comparison};
  \node[det] (d5) at ( 0.7,-0.2) {identity re-fetch and predecessor cross-signature};
  \node[det] (d2) at ( 3.5,-0.2) {key mismatch and proven warning absence};
  \node[det] (d4) at ( 6.1,-0.2) {cross-vantage fork check};
  \foreach \a/\d in {a1/d1,a3/d3,a5/d5,a2/d2,a4/d4}{%
    \draw[->,green!45!black,dashed] (\a) -- (\d);}

  % X1: out of scope, one line
  \node[oos] (x1) at (0.7,-1.7) {X1 endpoint compromise: outside the directory threat model};
\end{tikzpicture}%
}
