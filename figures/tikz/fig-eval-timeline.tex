% fig-eval-timeline.tex — extracted from ch6-evaluation.tex
% Two epoch positions, matching the adversarial demonstration: the A1a/A1b
% tests assert warn_epoch == attack_epoch + 1 (crate/tests/adversarial.rs).
% Detection happens under the attack head; the warning commits at the next one.
% Label: \label{fig:eval-timeline}
\begin{tikzpicture}[>=Stealth,font=\scriptsize]
  \draw[thick,black!40] (-0.3,0) -- (8.3,0);
  \foreach \x/\ep/\ev/\c in {%
     0/{Epoch $t$}/{key substituted and committed\\verification under this head\\detects the mismatch}/{red!70!black},%
     8/{Epoch $t{+}1$}/{warning committed\\the peer verifies it\\under the same head}/{green!50!black}}{%
     \filldraw[fill=white,draw=\c,thick] (\x,0) circle (5pt);
     \node[above=5pt,font=\scriptsize\bfseries,text=\c] at (\x,0) {\ep};
     \node[below=6pt,align=center] at (\x,0) {\ev};
  }
  \node[font=\itshape,text=black!55,align=center] at (4,-2.35) {positions from the demonstration\\ not a wall-clock or protocol-wide detection bound};
\end{tikzpicture}
