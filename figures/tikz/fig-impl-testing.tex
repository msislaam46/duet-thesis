% fig-impl-testing.tex — extracted from ch5-implementation.tex
% Five independent evidence classes, each supporting the implementation on its
% own (not a sequential pipeline): they converge on the implementation-evidence
% node rather than feeding one into the next.
% Label: \label{fig:impl-testing}
\begin{tikzpicture}[>=Stealth,font=\footnotesize,
   st/.style={draw,rounded corners,minimum width=4.6cm,minimum height=7mm,align=center}]
  \node[st] (rfc) {RFC test vectors (VRF, log proofs, HKDF)};
  \node[st,below=4mm of rfc] (unit) {unit + invariant tests (per module)};
  \node[st,below=4mm of unit] (xl) {cross-language vectors (Rust $=$ TypeScript)};
  \node[st,below=4mm of xl] (scen) {scenario suite (A0, A1a, A1b, A2--A5, X1)};
  \node[st,below=4mm of scen] (bench) {performance benchmarks (\cref{ch:eval})};
  \node[st,right=24mm of xl,fill=black!4] (ev) {implementation and\\evaluation evidence};
  \foreach \a in {rfc,unit,xl,scen}{\draw[->,shorten >=2pt] (\a.east) -- (ev.west);}
  \draw[->,dashed,shorten >=2pt] (bench.east) -- (ev.west);
\end{tikzpicture}
