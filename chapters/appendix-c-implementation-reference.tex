\chapter{Implementation Reference}\label{app:apiref}

This appendix documents the implementation-level interface details referenced from
\cref{ch:impl}: the directory server's REST API, the privileged testing interface, the
Signal Desktop integration hooks and user-interface events, the repository layout, and the
runner commands. None of it is needed to follow the design (\cref{ch:design}) or the
evaluation (\cref{ch:eval}). It is provided for reproducibility and for readers who wish to
run the system.

% ----------------------------------------------------------------------------
\section{Directory Server REST API}\label{sec:apiref-rest}

The directory server exposes a public API used by clients and auditors. Requests and
responses are JSON, and byte strings are hex-encoded. The verified-lookup endpoints return the
value together with the prefix proof, the log-inclusion proof, and the signed tree head, so
that the client can run the full verification chain (\cref{ssec:design-verify}).
\Cref{tab:apiref-public} lists these public endpoints.

\begin{table}[ht]
\centering
\caption{Public directory server endpoints.}
\label{tab:apiref-public}
\setlength{\extrarowheight}{2pt}
\footnotesize
\begin{tabular}{@{}lp{3.9cm}p{7.6cm}@{}}
\toprule
Method & Path & Purpose \\
\midrule
GET  & \texttt{/sth} & current signed tree head \\
GET  & \texttt{/search} & verified lookup by label, returning value, prefix proof, log-inclusion proof, and the signed STH \\
GET  & \texttt{/search-by-user-id} & lookup by user identifier, with the VRF proof attached \\
GET  & \texttt{/consistency} & consistency proof between two log sizes (\texttt{from}, \texttt{to}) \\
GET  & \texttt{/epoch} & current epoch number \\
GET  & \texttt{/log-root-at} & log root at a given tree size \\
GET  & \texttt{/epoch-delta} & a committed epoch's declared update set and committed directory root, so that a networked auditor can replay the transition (\cref{alg:auditor}) \\
GET  & \texttt{/vrf-public-key} & operator VRF public key \\
GET  & \texttt{/sth-public-key} & operator STH signing public key \\
POST & \texttt{/register} & register a user's key (server derives the VRF label) \\
POST & \texttt{/update-raw} & write a client-derived (dual-index) label$\to$value. The public surface is write-once, so an occupied label is rejected with \texttt{409 Conflict} (\cref{ssec:design-detection}) \\
POST & \texttt{/commit} & close the current epoch and sign a new STH \\
GET  & \texttt{/health} & liveness probe \\
\bottomrule
\end{tabular}
\end{table}

% ----------------------------------------------------------------------------
\FloatBarrier
\section{Privileged Testing Interface}\label{sec:apiref-admin}

A separate administrative API, bound to an internal-only port, is used exclusively by the
adversarial scenario suite to stage attacks. It is not part of the protocol. In the
prototype these endpoints are unauthenticated. They are test-only and would be omitted or
disabled in production. \Cref{tab:apiref-admin} lists them.

\begin{table}[ht]
\centering
\caption{Privileged testing endpoints for internal use.}
\label{tab:apiref-admin}
\setlength{\extrarowheight}{2pt}
\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{1.5cm}>{\raggedright\arraybackslash}p{4.2cm}>{\raggedright\arraybackslash}X@{}}
\toprule
Method & Path & Purpose \\
\midrule
POST & \texttt{/admin/update} & overwrite a slot's value, bypassing the public write-once policy. The overwrite is still declared in the epoch delta as a rotation (stages an A1 or A5 substitution) \\
POST & \texttt{/admin/delete} & remove a committed leaf \emph{without} declaring it in the epoch delta, so the auditor's transition replay fails (stages the suppression-by-deletion scenario) \\
POST & \texttt{/admin/commit} & commit an epoch \\
POST & \texttt{/admin/equivocate} & sign a second, divergent head at one epoch (stages the A4 split-view equivocation scenario) \\
POST & \texttt{/admin/stop-equivocate} & clear the forked head \\
POST & \texttt{/admin/reset} & rebuild the server from its fixed seed with a fresh genesis epoch, for repeatable demonstrations \\
\bottomrule
\end{tabularx}
\end{table}

% ----------------------------------------------------------------------------
\FloatBarrier
\section{Signal Desktop Integration Hooks and Events}\label{sec:apiref-hooks}

\Cref{tab:apiref-hooks} maps the integration concepts of \cref{sec:impl-signal} to the
corresponding prototype functions, and \cref{tab:apiref-events} maps the user-interface
outcomes to their DOM event names. The live client attaches at key upload and peer-key retrieval. It
records peer identities at the peer-key-retrieval point and during the periodic sweep. The
pre-key-bundle row is a documentation-only integration sketch that marks where a production binding
of the live X3DH secret would attach.

\begin{table}[ht]
\centering
\caption{Integration hooks.}
\label{tab:apiref-hooks}
\small
\begin{tabular}{@{}lll@{}}
\toprule
Lifecycle point & Prototype function & Layer \\
\midrule
key upload & \texttt{registerKeys} & TypeScript \\
peer-key retrieval & \texttt{getKeysForServiceId} & TypeScript \\
peer identity observation & \texttt{onPeerIdentityObserved} & TypeScript \\
pre-key-bundle processing & \texttt{hook\_process\_prekey\_bundle} & Rust (libsignal) \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{User-interface outcomes and their DOM events.}
\label{tab:apiref-events}
\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{3.5cm}X p{3.2cm}@{}}
\toprule
Presentation & Meaning & DOM event \\
\midrule
grey badge & verification pending & none \\
green badge & key verified & \texttt{kt:verified} \\
amber banner & directory server unreachable with the badge unchanged & \texttt{kt:unreachable} \\
red badge and banner &
proof failure, verified key mismatch, proof-verified warning-slot value, or operational timeout &
\texttt{kt:warning} \\
demonstration control & stage one CEA fork for the selected conversation & \texttt{kt:cea-inject} \\
\bottomrule
\end{tabularx}
\end{table}

The badge starts grey and changes to green only after a proof-backed \texttt{kt:verified} event for
the displayed conversation. Strict peer matching prevents an unattributable or stale green event
from clearing another conversation's warning. A \texttt{kt:warning} event uses the permissive warning
matcher and changes the badge to red while presenting the warning banner. The global
\texttt{kt:unreachable} event displays a separate amber availability banner without changing the
badge verdict. The demonstration-only \texttt{kt:cea-inject} event stages one CEA fork for the
selected conversation. \Cref{fig:apiref-ui-components} shows the five rendered components.

\begin{figure}[htbp]
\centering
\begin{minipage}[t]{0.30\textwidth}
  \centering
  \includegraphics[height=0.75cm]{figures/ui-component-pending.png}
  \par\smallskip
  \small (a) pending badge
\end{minipage}\hfill
\begin{minipage}[t]{0.30\textwidth}
  \centering
  \includegraphics[height=0.75cm]{figures/ui-component-verified.png}
  \par\smallskip
  \small (b) Verified badge
\end{minipage}\hfill
\begin{minipage}[t]{0.30\textwidth}
  \centering
  \includegraphics[height=0.46cm]{figures/ui-component-inject-fork.png}
  \par\smallskip
  \small (c) Demonstration control
\end{minipage}
\par\medskip
\includegraphics[width=0.64\textwidth]{figures/ui-component-unreachable.png}
\par\smallskip
\small (d) Availability banner
\par\medskip
\includegraphics[width=0.64\textwidth]{figures/ui-component-warning.png}
\par\smallskip
\small (e) Warning badge and banner
\caption[DUET user-interface components.]{DUET user-interface components. The header badge is grey
while verification is pending, green after a proof-backed result, and red after a warning. The amber
banner reports temporary directory unavailability without changing that verdict. The
demonstration-only control stages one CEA fork for the selected conversation.}
\label{fig:apiref-ui-components}
\end{figure}

% ----------------------------------------------------------------------------
\FloatBarrier
\section{Repository Layout and Runner Commands}\label{sec:apiref-repo}

The implementation is organized as follows:

\begin{itemize}
  \item \texttt{crate/}: the Rust \texttt{duet} crate, with library modules
  (\cref{tab:impl-modules}), the server and auditor binaries, and the benchmark
  harness.
  \item \texttt{demo/}: the PowerShell and Docker orchestration for the server, auditor, clients, and scripted attacks.
  \item \texttt{signal-desktop/ts/kt/}: the embedded Signal Desktop verifier and monitor modules, implemented in TypeScript, whose shared
  verifier modules are mirrored byte-for-byte by the \texttt{signal-desktop-kt/} verifier
  package, while integration wiring and test scaffolding differ between the two.
\end{itemize}

The system is driven by single commands: \texttt{cargo test} and \texttt{cargo bench}, run from
\texttt{crate/}, for the Rust tests and benchmarks, and the runner script
\texttt{demo/run.ps1} for the multi-actor demonstration. That script's native subcommands are
\texttt{server}, \texttt{auditor}, \texttt{build}, \texttt{client1}, \texttt{client2},
\texttt{attack}, \texttt{fork}, \texttt{delete}, \texttt{simulate-delete}, \texttt{stop-fork},
\texttt{stop}, and \texttt{reset}. \texttt{client1} and \texttt{client2} launch the two Signal
instances the two-client demonstration needs, and \texttt{reset} (equivalently \texttt{demo/reset.ps1})
returns the directory server to a fresh genesis tree between runs. Both Signal instances must then be
fully quit and relaunched so they re-register their keys and clear any staged in-memory one-shot CEA
injection, so each demonstration starts clean. A self-contained walk-through of the client
verifier, independent of Signal Desktop, runs from the \texttt{signal-desktop-kt/} package with
\texttt{npm run demo}. A containerized path (\texttt{up}, \texttt{logs}, \texttt{down}) runs the directory server,
auditor, and log viewer under Docker. The attacker runs only when invoked. The networked-in-app mode
is enabled by setting a server URL in the client's environment
before launch.

The client and auditor use a structured \texttt{[KT]} prefix to identify the component that
produced each log line. When applicable, client logs also identify the affected peer or label. Additional proof
details and attack targets are shown when the verbose or demonstration options are enabled.
Repeated monitor results are suppressed. A normal run reports a verification result again only
after its state changes.

% ----------------------------------------------------------------------------
\FloatBarrier
\section{Integration and Flow Diagrams}\label{sec:apiref-diagrams}

These diagrams accompany the client integration of \cref{sec:impl-signal}. \Cref{fig:impl-signal}
maps the runtime arrangement of the embedded client and the three processes it drives,
\cref{fig:impl-verify} traces the matching branch of the networked
\texttt{RemoteKTClient.detectMitm} verification path, and \cref{fig:impl-e2e} traces the normal and
A1b attack workflows across the four roles.

\begin{figure}[ht]
\centering
\input{figures/tikz/fig-impl-signal}
\caption[Signal Desktop integration points.]{Signal Desktop integration. The embedded Signal Desktop verifier communicates over HTTP with the DUET directory server. It registers identity material at the
key-upload hook and verifies peer material at the peer-key-retrieval hook, reporting verdicts to the
badge and banner. An auditor process polls the server for signed heads and epoch disclosures. During
the controlled A4 run, it also retrieves the staged second view. The attacker uses a privileged
testing interface. \Cref{tab:apiref-hooks,tab:apiref-events} list the hook functions and verdict-event names.}
\label{fig:impl-signal}
\end{figure}

\begin{figure}[H]
\centering
\input{figures/tikz/fig-impl-verify}
\caption[Owner-slot and warning-slot verification.]{Matching branch of
\texttt{RemoteKTClient.detectMitm}. The client verifies its own dual-index slot against a signed
head anchored to the append-only log. Only a matching owner-slot value proceeds to the proof-verified
warning-slot lookup. The live monitor handles registration and warning publication separately.}
\label{fig:impl-verify}
\end{figure}

\begin{figure}[p]
\centering
\input{figures/tikz/fig-impl-e2e}
\caption[Normal and A1b attack paths across the four roles.]{Normal and A1b attack paths across
the two clients, directory, and auditor. In the lower panel, Alice detects an own-slot mismatch and
writes a warning that Bob later reads. The auditor separately rejects the immutable-slot rotation.}
\label{fig:impl-e2e}
\end{figure}

\FloatBarrier
In \cref{fig:impl-e2e}, Alice and Bob each run an embedded verifier, while the directory server and auditor
execute as separate processes. In the normal path Alice and Bob each register their session slot and
commit. Bob's verifier
then verifies its owner slot, the shared warning slot, and the peer slot through three
label-bound, proof-verified lookups. These are its owner slot $\ell^{\B}_{\mathsf{self}}$, the shared warning slot $\ell_{\mathsf{warn}}$, which is verifiably
empty, and the peer slot $\ell^{\B}_{\mathsf{peer}}$ ($=\ell^{\A}_{\mathsf{self}}$) that
contains Alice's key. The monitor emits a verified green result, while the auditor polls and replays to
\textsc{Consistent}. In the
branch for the A1b session-slot substitution, the server overwrites Alice's own dual-index slot $\ell^{\A}_{\mathsf{self}}$ (which is also Bob's peer slot $\ell^{\B}_{\mathsf{peer}}$).
Alice's monitor compares the substituted value with her local identity key, finds a mismatch, and
writes the shared warning slot. Bob's monitor then reads that warning and both clients go red. Independently, the
auditor's transition replay rejects the declared immutable rotation, so detection does not rest on the
client path alone. The live run in \cref{ssec:impl-demo} shows this inside the real interface.

% ----------------------------------------------------------------------------
\FloatBarrier
\section{Demonstration Gallery}\label{sec:apiref-demo}

The supporting captures for the demonstration in \cref{ssec:impl-demo} show the honest baseline,
the privileged commands used to stage the attacks, the client-side detection paths, and the auditor
verdicts. The main-body figure presents the user-facing outcomes without developer tooling. The
captures here provide the corresponding console and terminal evidence. \Cref{fig:demo-baseline}
shows the clean A0 baseline, \cref{fig:demo-cmd} shows the commands used to stage the A1a global-slot and A1b session-slot substitutions,
\cref{fig:impl-screens-fronts,fig:impl-cea-front} show the client-side detection paths,
\cref{fig:demo-equivocation} shows the A4 equivocation command, and \cref{fig:impl-auditor} shows the auditor
verdicts. The A4 command and auditor captures come from separate reset runs, so their epoch values
do not form one timeline.

\begin{figure}[p]
\centering
\includegraphics[width=0.90\textwidth]{figures/Alice-normal-appendix.png}
\par\smallskip
\small (a) Alice's client viewing Bob
\par\medskip
\includegraphics[width=0.90\textwidth]{figures/Bob-normal-appendix.png}
\par\smallskip
\small (b) Bob's client viewing Alice
\caption[Honest baseline (A0).]{Honest A0 baseline. Both clients display the green verified badge
after the account-wide identity check and conversation check succeed. Bob's console also records one
expected pending result before the peer entry appears and is then verified.}
\label{fig:demo-baseline}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.86\textwidth]{figures/auditor-rotations-attack.png}
\caption[Commands staging the A1a global-slot and A1b session-slot substitutions.]{Commands used to stage the A1a global-slot and A1b session-slot substitutions in one controlled run.
The first replaces the mutable VRF-derived identity entry and commits epoch 5. The second replaces
the immutable per-conversation lookup slot and commits epoch 7. The corresponding auditor outcomes
appear in the upper panel of \cref{fig:impl-auditor}.}
\label{fig:demo-cmd}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=0.90\textwidth]{figures/Bob-vrf-attacked-appendix.png}
\par\smallskip
\small (a) Bob's account-wide self-check
\par\medskip
\includegraphics[width=0.90\textwidth]{figures/alice-vrf-attacked-appendix.png}
\par\smallskip
\small (b) Alice's verification of the peer warning
\caption[A1a self-detection and peer propagation.]{Client-side evidence for the A1a global-slot substitution. Bob's account-wide
self-check detects that his global identity entry no longer matches the local key and publishes the
pair-bound warning. Alice then verifies that warning for the conversation. Both clients display the
red warning state.}
\label{fig:impl-screens-fronts}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.90\textwidth]{figures/cea-bob-appendix.png}
\caption[CEA fork detection and warning.]{Continuous CEA detection after a one-shot ratchet-key
injection. The verifier establishes the \textsc{Fork} verdict independently of the
warning-publication outcome, then publishes the authenticated in-band warning (here confirmed), and
displays the red warning state for the selected conversation.}
\label{fig:impl-cea-front}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.86\textwidth]{figures/cmd-equivocation.png}
\caption[Staging A4 equivocation.]{Staging A4 equivocation. The privileged testing endpoint signs a
second root at the current tree size. The auditor detects the two validly signed roots by comparing the primary and staged second views,
as shown in the lower panel of \cref{fig:impl-auditor}.}
\label{fig:demo-equivocation}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[width=0.88\textwidth]{figures/auditor-rotations.png}
\par\smallskip
\small (a) A1a and A1b transition-audit run
\par\medskip
\includegraphics[width=0.88\textwidth]{figures/auditor-fork.png}
\par\smallskip
\small (b) A4 fork verdict from the staged second view
\caption[Auditor outcomes for A1a, A1b, and A4.]{Auditor outcomes for the controlled attacks. The
upper capture belongs to the run in \cref{fig:demo-cmd}. The auditor verifies the declared history
through epoch 6, including the mutable identity-entry rotation and the subsequent valid update. It
then rejects the immutable-slot replacement at epoch 7 because replay does not reproduce the
committed state. The lower capture shows the A4 equivocation in a separate reset run, where two validly signed roots
exist at one tree size.}
\label{fig:impl-auditor}
\end{figure}

\FloatBarrier

% ============================================================================
\section{Implementation and Engineering Details}\label{sec:apiref-engineering}

This section provides the module, server, integration, and engineering detail summarized in
\cref{ch:impl}.

\subsection{Directory State, Cryptography, and Server}\label{ssec:apiref-eng-core}

The directory's state lives entirely in memory, in the cooperating structures that parallel the
authenticated-data-structure stack of the design (\cref{ssec:design-structures}). The base
\texttt{hashing} module fixes the RFC~6962 leaf and node domain tags shared by both structures.
The \texttt{merkle\_log} module maintains the append-only log as a vector of hashes and implements
the RFC~9162 inclusion and consistency verifiers, each proof costing time logarithmic in the
number of entries. The \texttt{prefix\_tree} module implements the directory itself as a
path-compressed sparse Merkle tree, held as a pointer-based structure so that only the nodes
actually populated are materialized. Each compressed leaf lazily memoizes the top of its
empty-sibling hash chain, so that serving a proof whose copath meets a compressed leaf does not
recompute the up-to-256-level chain on every lookup. A single \texttt{Arc<Mutex<KTServer>>} protects the server state, so reads and writes are serialized.
The prototype does not shard or replicate this state.

The \texttt{vrf} module implements the
ECVRF-EDWARDS25519-SHA512-TAI construction of RFC~9381 (suite \texttt{0x03}). The \texttt{dual\_index} module
derives the three labels $\ell_{\mathsf{self}}$, $\ell_{\mathsf{peer}}$, and $\ell_{\mathsf{warn}}$
from the shared secret by HKDF-SHA256 (RFC~5869), exactly as specified in
\cref{ssec:design-labels}, and the \texttt{cea} module implements the continuous example construction
of the ACKA framework~\cite{dowling2023acka}. The crates
\texttt{sha2}, \texttt{hkdf}, \texttt{ed25519-dalek}, and \texttt{curve25519-dalek} supply the standard
primitives, and the ECVRF of RFC~9381 is the one primitive implemented directly, checked against the published
suite-\texttt{0x03} test vectors.

The HTTP server, built on the
\texttt{axum} framework over the asynchronous \texttt{tokio} runtime, wraps the \texttt{KTServer}
and serves the lookup, monitoring, and consistency operations as JSON, alongside a privileged
testing interface used only by the adversarial scenario suite and the scripted attacker. An auditor binary, implemented as a small \texttt{ureq} HTTP client, verifies signed-head growth and
replays declared epoch transitions against a temporary copy of its accepted directory state. The server enforces the write-once policy in its raw-label
update path, so a write to an already-occupied dual-index, CEA, or warning label is rejected (the public
\texttt{/update-raw} endpoint answers \texttt{409 Conflict}), while the privileged interface exposes
an explicit unchecked overwrite so the adversarial suite can play a malicious operator. Every commit
freezes a \emph{declared update set} that the server publishes at an \texttt{/epoch-delta} endpoint,
which the auditor consumes (\cref{fig:impl-transition-audit}) by replaying each epoch's declared set
against that temporary copy and accepting only if the replayed root equals the committed root. Its head-comparison step keeps a fork distinct from an unverified consistency proof. Two validly
signed roots at one tree size are reported as \texttt{[alert] fork: two validly signed roots exist
at one tree size}. A required proof that is missing, malformed, or invalid yields \texttt{[alert]
consistency unverified: required proof is missing, malformed, or invalid} and retains the baseline.
The auditor prints each verdict once and re-emits a line only when the verdict or signed head changes.
The demonstration supplies the second head through \texttt{view=b} on the same server. A deployment
would obtain that signed head from an independent source, following the third-party auditor role in
the IETF KeyTrans protocol~\cite{ietf-keytrans-protocol}.
Independently of any auditor, the \texttt{monitor} module retains a writer-side persistence proof
(the inclusion proof and signed tree head from the epoch in which a warning commits) as an available
capability, so that a later removal yields a bundle verifying against the server's own signed heads.
The client's live detection (\cref{alg:detect}) does not depend on it.
Authenticated ownership of raw-label writes is not implemented, so without account-layer authorisation a
malicious client could occupy an unclaimed peer slot before its legitimate owner writes to it. The one
core cost that is not sublinear is the epoch commit, which snapshots the tree by a deep clone.

\subsection{Signal Desktop Integration Wiring}\label{ssec:apiref-eng-wiring}

The integration processes account identifiers (ACIs) only. It uses libsignal to convert each ACI
service-ID string to its canonical 16 raw UUID bytes before registration, verification, monitoring,
or CEA processing. Phone-number identifiers (PNIs) are excluded before these operations and
therefore produce no lookup verdict, user-interface event, or monitoring state.

The integration uses key-upload and peer-key-retrieval hooks together with a background monitor.
Peer identity material is recorded during key retrieval and through a periodic identity-record
sweep. That sweep tracks the still-active direct conversations, dropping those that have gone
inactive (their last-active marker cleared, as when a conversation is deleted) while continuing to
monitor archived ones, which keep that marker set. No live session-establishment hook
exposes the X3DH shared secret. The
peer-key-retrieval hook runs the full verification chain
(\cref{ssec:design-verify}) on a peer's key at the peer-key-retrieval integration point and reports the verdict without
blocking session establishment. The monitor keys its per-conversation state by the derived label
rather than by any transient handle, so a verdict is bound to the slot it was computed for. Verdicts
appear in the user interface through a conversation-header badge and a
banner, driven by DOM events. The specific hook functions and event names are
listed in \cref{tab:apiref-hooks,tab:apiref-events}.
\Cref{fig:impl-verify} illustrates the matching branch of the networked \texttt{RemoteKTClient.detectMitm}
path: the client reads its own dual-index lookup slot $\ell_{\mathsf{self}}$, verifies the prefix
proof, log-inclusion proof, and signed tree head, and only on a verified owner slot issues a second
lookup for the shared warning slot. When both checks pass, the method returns \texttt{null}. It returns a
\texttt{MitmWarning} if the owner slot is absent or mismatched or the warning slot is occupied, and a
proof or head-anchoring failure throws. The live background monitor implements the
corresponding checks independently in \texttt{monitorConversation} (\cref{alg:detect}).

\subsection{Electron, Determinism, and Fail-Closed Behaviour}\label{ssec:apiref-eng-robust}

Three Electron obstacles arose. First, the bundler splits a chunk at every dynamic import, so every
dynamic import for the key-transparency code was removed and the whole layer now lives in one bundle,
with the networked path made opt-in and its failure caught separately. Second, with context isolation
enabled, property assignments to \texttt{window} in the preload world are not visible to the renderer,
so objects injected through the console had no effect, whereas DOM custom events on \texttt{window}
pass from the preload context to the renderer. Finally, the application exposes both a typed event bus and DOM custom events.
Dispatching verdicts through both interfaces resolved the delivery mismatch. Several robustness issues appeared only end to end: a
server that generated a fresh signing key on each launch invalidated cached public keys, fixed by
deriving the server's keys deterministically from a seed. A panic on a freshly emptied log poisoned the
shared mutex, resolved by bootstrapping a genesis epoch at initialization. An administrative reset
that rebuilds the server from its seed makes the demonstration repeatable.

On the client, the trusted computing
base is the verifier implementation, its cryptographic dependencies, and the directly implemented
ECVRF (\cref{ssec:impl-core-crypto}), together with the operator's two public keys, the STH signing
key and the VRF key. Because the prototype obtains these keys from the service during initialization,
their initial authenticity remains a trust assumption, and independent authenticated provisioning is a
production requirement. Subject to that assumption, the server, network, and log are not trusted, and
each lookup response is verified before its value is used. The verifier is fail-closed for integrity checks: any cryptographic or
proof-validation failure yields a reject verdict rather than falling through to acceptance. The present
key-fetch hook reports explicit mismatches and proof failures as a red warning but does not abort bundle
processing, so complete error-to-UI routing and session-blocking enforcement remain prototype
limitations (\cref{ch:discussion}). Production request handlers must also recover from poisoned locks
instead of relying only on the genesis-epoch bootstrap.
