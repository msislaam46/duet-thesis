\chapter{Legal, Social, Ethical and Professional Issues}\label{ch:lsepi}

DUET processes account-linked key material, extends AGPL-covered software, and reports security
evidence that may influence deployment decisions. These activities raise legal, social, ethical, and
professional responsibilities, which this chapter examines in the context of the project.

% ============================================================================
\section{Legal Issues}\label{sec:lsepi-legal}

The client work derives from libsignal and Signal-Desktop, both distributed under the
AGPL-3.0~\cite{libsignal,signal-desktop,agpl3}. Conveying modified AGPL-covered code requires the
corresponding source to be provided under the licence. If a modified version is operated as a network
service that supports remote interaction, Section~13 additionally requires that interacting users be
offered the corresponding source. This prototype was evaluated locally rather than operated as a
public service. UK law protects software as a literary work, so upstream copyright and licence notices are retained rather than
rewritten~\cite{uk-software-copyright}. Upstream contribution would require a contributor
licence agreement with the Signal Foundation~\cite{signal-foundation-structure,signal-cla}, a route
not taken here.

The project uses publicly documented cryptographic algorithms and open-source implementations. Public
availability does not by itself determine export-control classification, so this thesis makes no claim
that the implementation falls within a particular exemption~\cite{uk-cryptonote,uk-export-lists}. A
production release would require classification against the applicable control list and the guidance
current at that time. The directory stores pseudonymised identifiers, public keys, and epoch metadata,
never message content or plaintext identifiers. Pseudonymised data remains personal data under UK
GDPR, so this design is treated as processing personal data rather than
exempt~\cite{ico-pseudonymisation}. VRF-derived indices keep raw identifiers out of the published
tree and reduce direct identifier exposure, but they do not make the records anonymous. Production
use would require a data-protection assessment, a documented processing purpose and lawful basis,
restricted access to warning records, and defined retention periods. Two residual exposures remain,
the query pattern visible to the operator and the density metadata a bitmap-compressed proof reveals,
and both are stated in \cref{ch:discussion}~\cite{ico-data-principles}.

% ============================================================================
\section{Social and Ethical Issues}\label{sec:lsepi-social}

Undetected key substitution is a physical risk for journalists, activists, and others whose safety
depends on a conversation staying private. These are among the users an automatic detector is
intended to protect, which makes accuracy about what was built an ethical obligation. This
implementation runs on a single machine and in memory and uses a deterministic conversation-secret
stand-in and a seeded ratchet simulation (\cref{sec:impl-strategy}). It does not protect a client
whose endpoint is compromised (\cref{sec:threat-assumptions}). Overstating a security tool puts its
intended users at risk.

Warnings can themselves cause harm when they are ambiguous, excessive, or disclose sensitive
information. A red warning a user cannot interpret produces alarm fatigue rather than protection,
which is why an unreachable directory server raises an amber notice and only a verified mismatch
raises a red warning. A red warning reports that an integrity or consistency check failed. It does not by
itself assign a cause to that failure. Usability
testing, accessibility, escalation routes, and support paths are unaddressed here and named as future
work in \cref{ch:discussion}. Warning records are also new data about who was warned and when, so the
capability that protects a user creates a fresh metadata surface.

All evaluation used synthetic accounts (\cref{ssec:impl-demo}). The evaluation did not recruit
participants or intercept live traffic and was not conducted as a human-participant study. The
infrastructure, energy, and access costs of an independent second vantage were not measured, and any
deployment depending on one should measure them.

% ============================================================================
\section{Professional Issues and Codes of Conduct}\label{sec:lsepi-prof}

The BCS Code of Conduct organizes obligations into public interest, professional competence and
integrity, duty to relevant authority, and duty to the profession~\cite{bcs-code}. The public
interest is served by stating the limits above as prominently as the results. Competence and
integrity are served by verifying rather than trusting the one primitive written by hand, and by
disclosing in \cref{sec:impl-challenges} the defects corrected during development, including a
missing signed-head binding a passing test suite had not caught. Regular supervisory review, source
attribution, and compliance with the university's academic-integrity requirements address the duty to
the relevant authority. Separating measured prototype results from unimplemented production
requirements addresses the duty to the profession.

The IET Rules of Conduct point the same way~\cite{iet-conduct}. Rule 2 requires accuracy and rigour,
which is why every measurement in \cref{ch:eval} carries its scope. Rules 5 and 6 require working
within competence and maintaining knowledge, which is why the hand-written ECVRF is checked against
the RFC~9381 vectors rather than assumed correct. Rule 26 requires that opinions state their
limitations, which is why the thesis states the assumptions and qualifications attached to its
security and performance claims. No defect affecting deployed Signal software was found. Any such
defect would have been reported through Signal's security vulnerability channel rather than
published~\cite{signal-security-report}.

The resulting responsibilities and project responses are summarized in \cref{tab:lsepi-mapping}.

\begin{table}[ht]
\centering
\caption{Legal, social, ethical, and professional instruments and considerations, their relevance to
the project, and the project's response.}
\label{tab:lsepi-mapping}
\footnotesize
\setlength{\extrarowheight}{2pt}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{2.5cm}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X@{}}
\toprule
Instrument or consideration & Relevance & Implication & Project response \\
\midrule
AGPL-3.0 (libsignal, Signal Desktop) & the prototype extends AGPL-covered Signal code & conveying modified code engages the corresponding-source obligations, and offering a modified network-interactive version as a remote service would engage AGPL Section~13 & the prototype was not run as a public service, the upstream notices are preserved, and any upstream contribution would follow Signal's contributor process \\
UK GDPR (pseudonymised account data) & a production directory would associate keys and labels with accounts & such pseudonymised information remains personal data for an operator holding the linking information & benchmarks used synthetic inputs, and production would require a data-protection assessment, restricted access, and retention periods \\
UK export controls (cryptographic software) & the design composes standard cryptographic primitives & classification depends on functionality, destination, and end use, and public specifications do not by themselves determine it & no export-control classification is attempted \\
BCS Code of Conduct & public interest, competence, relevant authority, and profession & duties on security and privacy, competence, honesty to the supervisor, and not overstating results & established libraries, RFC test vectors, reproducible tests, supervisory review, and explicit scope \\
IET Rules of Conduct & accuracy and rigour, working within competence, and qualified opinions & reported results must state their limitations and stay within competence & adversarial tests, cross-language vectors, reproducibility measures, and explicit scope qualifications \\
Research ethics (synthetic data) & adversarial functionality was exercised in a local test harness & no live interception and no real users & synthetic accounts only, with no personal data processed \\
\bottomrule
\end{tabularx}
\end{table}
