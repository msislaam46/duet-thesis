\chapter{Traceability Matrix}\label{app:traceability}

This appendix traces the thesis's principal claims from motivation to evidence. For traceability, this
matrix uses four evaluation questions. RQ1 asks whether a key substitution is detected, in band, once
monitoring begins. RQ2 asks whether adding that detection avoids enlarging an individual lookup proof,
and what the measured proof sizes and local component timings indicate about the component cost of
detection. RQ3 asks whether offline catch-up is bounded independently of the number of missed epochs.
RQ4 asks whether the client verifier can integrate into a real messaging-client codebase.
\Cref{tab:trace-rq} links each question to the corresponding design claim, mechanism, tests,
benchmarks, and evidence. \Cref{tab:trace-contrib} organizes the same evidence by the four
contributions of \cref{sec:intro-contributions}.

\begin{landscape}
\begin{table}[htbp]
\centering
\caption[Research-question traceability.]{Research question $\to$ claim $\to$ mechanism $\to$ test $\to$ benchmark $\to$ evidence.}
\label{tab:trace-rq}
\setlength{\extrarowheight}{0pt}
\renewcommand{\arraystretch}{0.9}
\setstretch{1}
\footnotesize
\begin{tabular}{@{}L{1.6cm}L{4.3cm}L{3.7cm}L{5.4cm}L{1.7cm}L{5.4cm}@{}}
\toprule
RQ & Design claim & Mechanism & Test / scenario & Benchmark & Evidence \\
\midrule
RQ1 & substitution is detected, in band (bidirectional for A1/A5 after honest establishment; local divergent-label plus global VRF comparison for A3) &
dual-index warning scheme for A1; local divergent-label detection and the global VRF-anchored entry for A3 (\cref{sec:design-dual}); identity re-fetch and predecessor cross-signature for A5; cross-vantage auditor for A4 (\cref{sec:design-acka}) &
scenarios A1--A5 (\cref{tab:tests-adversarial}); dual-index derivation tests (\cref{tab:tests-rust-prim}) &
M17 & \cref{sec:eval-security}: scenarios A1--A5 establish detection; M17 asserts non-alarm on every clean workflow \\
RQ2 & detection does not enlarge an individual per-lookup proof; median full verification remains below \SI{150}{\micro\second} and grows far more slowly than $N$ &
combined-tree verification chain (\cref{ssec:design-verify}) &
prefix-tree, VRF, Merkle-log tests (\cref{tab:tests-rust-prim,tab:tests-rust-server}) &
M1--M3, M7, M11, M16 & \cref{sec:eval-micro,ssec:eval-scale}: 817\,B @ $10^6$; verify \SI{139.2}{\micro\second} at $10^7$; \SI{27.8}{} and \SI{31.1}{\micro\second} for synthetic Rust reconstructions of the one- and two-lookup detector branches (M16); M11 measures one signed-head lookup verification in Rust; the TypeScript client path was not timed (\cref{sec:eval-deploy}) \\
RQ3 & offline catch-up costs one proof, logarithmic rather than linear in the window &
windowed consistency proof (\cref{sec:design-windowed}, \cref{lem:windowed-size}) &
Merkle-log exhaustive-consistency and windowed-consistency tests (\cref{tab:tests-rust-prim,tab:tests-rust-server}) &
M4 & \cref{ssec:eval-catchup}: 320\,B @ $W{=}720$, ratio $\approx W$ against the synthetic current-head-anchored sequential baseline \\
RQ4 & the client verifier integrates into a real messaging-client codebase (Signal Desktop) as a prototype client warning path that does not block session establishment &
TypeScript verifier + lifecycle hooks (\cref{sec:impl-signal}) &
Jest wire-parity and unit suites (\cref{sec:tests-ts}); live demonstration (\cref{ssec:impl-demo}) &
Not applicable & \cref{sec:impl-signal,ssec:impl-demo}: the verifier runs inside a fork of Signal Desktop with lifecycle hooks, and the live two-client demonstration establishes client integration (cost under RQ2) \\
\midrule
Supporting & equivocation (split-view) is caught from a second vantage &
cross-vantage auditor (\cref{sec:design-acka}) &
scenario A4; auditor equivocation tests (\cref{sec:tests-rust}) &
M12 & \cref{ssec:eval-auditor}: \SI{27.1}{\micro\second}/check (median; local one-head fork check) \\
Supporting & continuous mode trades write volume for finer detection &
CEA continuous mode (\cref{ssec:design-modes}) &
M13 mode benchmark (\cref{sec:bench-modes}) &
M13 & \cref{ssec:eval-modes}: bounded above by $720\times$ \\
Supporting & a committed warning cannot be removed silently &
write-once policy + transition audit + persistence evidence (\cref{ssec:design-detection}) &
write-once and undeclared-removal tests (\cref{tab:tests-rust-server}); transition-audit and deletion-evidence tests (\cref{tab:tests-rust-system}) &
None & \cref{sec:eval-security}: post-commit deletion is tampering (not A2 censorship), closed by write-once policy, per-epoch transition audit, and persistence evidence \\
\bottomrule
\end{tabular}
\end{table}
\end{landscape}

\begin{table}[ht]
\centering
\caption{Contribution kind $\to$ output $\to$ primary evidence.}
\label{tab:trace-contrib}
\setlength{\extrarowheight}{2pt}
\footnotesize
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{2.1cm}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X@{}}
\toprule
Contribution & Output & Primary evidence \\
\midrule
Conceptual & dual-index warning scheme; ACKA-log instantiation (\cref{sec:design-dual,sec:design-acka}) & expected in-band verdicts in the enumerated dual-index warning cases; the implemented directory and auditor exercise ACKA's secure-log interface (\cref{sec:eval-security}) \\
Cryptographic & windowed consistency proof: RFC~9162 distant-head proof applied to offline monitoring, size bound (\cref{lem:windowed-size}); dual-index same-head consistency lemma as supporting analysis (\cref{lem:dualindex-consistency}) & same-head label consistency shown from proof soundness and collision resistance; a complete formal security proof remains future work; \num{320}\,B raw path bytes at $W{=}720$, one proof rather than $W$, size logarithmic in endpoint log sizes (\cref{ssec:eval-catchup}) \\
Systems & Rust \texttt{duet} crate, directory-server process, and independent auditor; TypeScript verifier in Signal Desktop (\cref{ch:impl}) & agreement with the Rust reference on the shared cross-language test vectors; live cross-process demonstration (\cref{ssec:impl-demo}) \\
Empirical & benchmark evaluation M1--M17; STRIDE/MITRE evaluation (\cref{ch:eval,app:security}) & per-lookup proof not enlarged by detection (the warning check reuses the same prefix-tree and log-proof structure without the VRF component); median verify below \SI{150}{\micro\second} (\cref{sec:eval-micro}); at system level, the expected detection verdict across the full adversarial suite A1--A5, which draws on the additional divergent-label/VRF, re-fetch, cross-signature, and cross-vantage controls (\cref{sec:eval-security}) \\
\bottomrule
\end{tabularx}
\end{table}
