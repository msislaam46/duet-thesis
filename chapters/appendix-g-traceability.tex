\chapter{Traceability Matrix}\label{app:traceability}

This appendix links the research questions of \cref{sec:intro-aims} to the design claim each makes,
the mechanism that delivers it, the test or scenario that checks it, and the benchmark that measures
it, so that every claim in the thesis can be traced from motivation to evidence. \Cref{tab:trace-rq}
gives the research-question view; \cref{tab:trace-contrib} maps the four contribution kinds to their
strongest supporting evidence.

\begin{table}[ht]
\centering
\caption{Research question $\to$ claim $\to$ mechanism $\to$ test $\to$ benchmark $\to$ evidence.}
\label{tab:trace-rq}
\footnotesize
\begin{tabular}{@{}L{0.7cm}L{2.9cm}L{2.4cm}L{3.1cm}L{1.7cm}L{2.8cm}@{}}
\toprule
RQ & Design claim & Mechanism & Test / scenario & Benchmark & Evidence \\
\midrule
RQ1 & substitution is detected automatically, in band, by both parties &
dual-index warning scheme (\cref{sec:design-dual}) &
scenarios A1--A5 (\cref{tab:tests-adversarial}); \idt{dual\_index} label tests (\cref{tab:tests-rust-prim}) &
M5, M6 & \cref{sec:eval-security}: all scenarios pass, FP $=0$ \\
RQ2 & detection does not enlarge an individual per-lookup proof; verification is cheap and scale-independent &
combined-tree verification chain (\cref{ssec:design-verify}) &
\idt{prefix\_tree}, \texttt{vrf}, \idt{merkle\_log} tests; cross-language vectors (\cref{app:tests}) &
M1--M3, M7 & \cref{sec:eval-micro,ssec:eval-scale}: 817\,B @ $10^6$; $\approx$\SI{132}{\micro\second} flat \\
RQ3 & offline catch-up costs one proof, logarithmic rather than linear in the window &
windowed consistency proof (\cref{sec:design-windowed}, \cref{lem:windowed-size}) &
\idt{merkle\_log} exhaustive consistency; \idt{windowed\_consistency} (\cref{tab:tests-rust-prim,tab:tests-rust-server}) &
M4 & \cref{ssec:eval-catchup}: 320\,B @ $W{=}720$, ratio $\approx W$ \\
RQ4 & the scheme integrates into a real deployed client at practical cost &
TypeScript verifier + lifecycle hooks (\cref{sec:impl-signal}) &
Jest wire-parity and unit suites (\cref{sec:tests-ts}); live demonstration (\cref{ssec:impl-demo}) &
M11 & \cref{sec:eval-deploy}: microseconds/conversation, runs in Signal Desktop \\
\midrule
--- & equivocation (split-view) is caught from a second vantage &
cross-vantage auditor (\cref{sec:design-acka}) &
scenario A4; auditor equivocation tests (\cref{sec:tests-rust}) &
M12 & \cref{ssec:eval-auditor}: \SI{27.6}{\micro\second}/check \\
--- & continuous mode trades write volume for finer detection &
CEA continuous mode (\cref{ssec:design-modes}) &
M13 mode benchmark (\cref{sec:bench-modes}) &
M13 & \cref{ssec:eval-modes}: plateau at $720\times$ \\
--- & a committed warning cannot be removed silently &
write-once policy + transition audit + persistence evidence (\cref{ssec:design-detection}) &
\idt{update\_raw\_is\_write\_once}, \idt{attack\_remove\_raw\_is\_not\_declared} (\cref{tab:tests-rust-server}); transition-audit and deletion-evidence tests (\cref{tab:tests-rust-system}); scenario A2 &
--- & \cref{sec:eval-security}: A2 detected \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{Contribution kind $\to$ artefact $\to$ strongest evidence.}
\label{tab:trace-contrib}
\footnotesize
\begin{tabular}{@{}p{2.1cm}p{6.0cm}p{4.6cm}@{}}
\toprule
Contribution & Artefact & Strongest evidence \\
\midrule
Conceptual & dual-index warning scheme; ACKA-log instantiation (\cref{sec:design-dual,sec:design-acka}) & automatic bidirectional detection, full adversarial suite passes (\cref{sec:eval-security}) \\
Cryptographic & windowed consistency proof + size bound; dual-index detection experiment (\cref{lem:windowed-size,def:dualindex-detect}) & 320\,B catch-up at $W{=}720$, one proof regardless of window (\cref{ssec:eval-catchup}) \\
Systems & Rust \texttt{kt} crate, server, auditor; TypeScript verifier in Signal Desktop (\cref{ch:impl}) & byte-for-byte parity; live cross-process demonstration (\cref{ssec:impl-demo}) \\
Empirical & benchmark campaign M1--M13; STRIDE/MITRE evaluation (\cref{ch:eval,app:security}) & per-lookup proof not enlarged by detection (warning check is an additional lookup of the same shape); sub-\SI{150}{\micro\second} verify (\cref{sec:eval-micro}) \\
\bottomrule
\end{tabular}
\end{table}
